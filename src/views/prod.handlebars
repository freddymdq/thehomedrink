<div class="container">
    <h2>{{product.title}}</h2>
  <div class="gallery">
      <div class="product">
        <img src="{{product.img}}" alt="{{products.title}}">
        <p>{{product.description}}</p>
        <p>Price: {{product.price}}</p>
        <p>Stock: {{product.stock}}</p>
        <button> COMPRAR </button>
      </div>
  </div>
</div>

